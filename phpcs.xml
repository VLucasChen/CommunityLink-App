<?xml version="1.0"?>
<ruleset name="App">
    <config name="installed_paths" value="../../cakephp/cakephp-codesniffer,../../slevomat/coding-standard"/>
    <config name="ignore_warnings_on_exit" value="1"/>

    <rule ref="CakePHP"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint.MissingNativeTypeHint">
        <exclude-pattern>*/src/Controller/*</exclude-pattern>
    </rule>

    <!-- PHPCBF mangles this file when auto-fixing; exclude until reformatted. -->
    <exclude-pattern>src/Controller/UsersController\.php</exclude-pattern>

    <file>src/</file>
    <file>tests/</file>
</ruleset>
